const paginatorPortionSize = 15;
const paginatorNextBtn = 'NEXT';
const paginatorPrevBtn = 'PREV';
const paginatorTitle = 'Pages';

export {
  paginatorPortionSize,
  paginatorNextBtn,
  paginatorPrevBtn,
  paginatorTitle,
};
